curl -X PUT "localhost:9200/usreldoc2?pretty" -H 'Content-Type: application/json' -d'
{
  "settings": {
    "number_of_shards": 4,
    "number_of_replicas": 1
  }
}
'

curl -X PUT "localhost:9200/usreldoc2?pretty" -H 'Content-Type: application/json' -d'
{
  "settings": {
    "number_of_shards": 4,
    "number_of_replicas": 1
  },
  "mappings": {
    "properties": {
      "date": {
        "type":   "date",
        "format":   "yyyy-MM"
      }
    }
  }
}
'


